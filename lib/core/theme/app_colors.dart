import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // =========================
  // Brand Colors
  // =========================

  /// Primary Brand (Premium Navy)
  static const primary = Color(0xFF1E293B);

  /// Secondary Brand (Indigo)
  static const secondary = Color(0xFF4F46E5);

  /// Accent (Teal)
  static const accent = Color(0xFF14B8A6);

  /// Premium Highlight
  static const premium = Color(0xFFF59E0B);

  // =========================
  // Dashboard Colors
  // =========================

  static const blue = Color(0xFF2563EB);
  static const purple = Color(0xFF7C3AED);
  static const cyan = Color(0xFF06B6D4);
  static const green = Color(0xFF22C55E);
  static const orange = Color(0xFFF97316);
  static const red = Color(0xFFEF4444);
  static const redWarring = Color(0xFFAF0303);
  static const grey = Color(0xFF989797);

  // =========================
  // Status Colors
  // =========================

  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFDC2626);
  static const info = Color(0xFF0EA5E9);

  // =========================
  // Backgrounds
  // =========================

  static const background = Color(0xFFF8FAFC);
  static const scaffold = Color(0xFFF1F5F9);
  static const card = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF8FAFC);

  // =========================
  // Text Colors
  // =========================

  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF475569);
  static const textHint = Color(0xFF94A3B8);
  static const textLight = Color(0xFFFFFFFF);

  // =========================
  // Borders & Divider
  // =========================

  static const border = Color(0xFFE2E8F0);
  static const divider = Color(0xFFCBD5E1);
  static const inputFocusedBorder = Color(0xFF4F46E5);
  static const listTileSelected = Color(0xFFEEF2F6);
  static const dialogBarrier = Color(0x990F172A);


  // =========================
  // Misc
  // =========================

  static const white = Colors.white;
  static const black = Colors.black;
  static const transparent = Colors.transparent;
  static const shadow = Color(0xE0E0E0FF);


  // =========================
  // Light Status Backgrounds
  // =========================

  static const successLight = Color(0xFFDCFCE7);
  static const warningLight = Color(0xFFFEF3C7);
  static const dangerLight = Color(0xFFFEE2E2);
  static const infoLight = Color(0xFFE0F2FE);

  // =========================
  // Sidebar
  // =========================

  static const sidebar = Color(0xFF0F172A);
  static const sidebarSelected = Color(0xFF334155);
  static const sidebarHover = Color(0xFF1E293B);

  // =========================
  // Role Colors
  // =========================

  static const superAdmin = Color(0xFF7C3AED);
  static const schoolAdmin = Color(0xFF2563EB);
  static const teacher = Color(0xFF14B8A6);
  static const student = Color(0xFFF59E0B);
  static const parent = Color(0xFFEC4899);
}